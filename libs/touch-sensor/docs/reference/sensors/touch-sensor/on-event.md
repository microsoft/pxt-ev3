# On Event

```sig
sensors.touch1.onEvent(ButtonEvent.Released, function () { })
```

# Parameters

## Examples


```blocks
sensors.touch1.onEvent(ButtonEvent.Released, function () {
    brick.showImage(images.expressionsSick)
})
```
